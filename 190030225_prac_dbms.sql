select c.cust_id,c.cust_name from customers c inner join branch b on b.c_id=c.cust_id;
select cust_name,city,ph_no((year,dob,getdate() as Age cutomers innnerjoin(select min[dob]y) a on a.y=dob);
select veh_name from vehicle;
select distinct city from curtsomer;
select sum(amount_per_seat) total amount from contract_permisiion;